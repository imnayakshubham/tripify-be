"""Prompt text, one module per agent.

Kept separate from the agents so a prompt can be read and tuned without
scrolling past control flow. The behavioural constraints from the brief live
here, except where they are enforceable in code — see app/agents/destination.py
and app/agents/budget.py.
"""
